from cell import Cell

class Player:
    speed = 1
    x = 10
    y = 10 
    
    
def MoveRight(self):
    self.x = self.x + self.speed
    
def MoveLeft(self):
    self.x = self.x + self.speed

  
def MoveUp(self):
    self.x = self.x + self.speed
    
def MoveDown(self):
    self.y =  self.y + self.speed